"""

File Name: __init__.py
Folder: teacher_marketplace/apps/__init__.py
Purpose: Marks the top-level apps/ directory itself as a Python package, so apps.common, apps.accounts, etc. are importable as dotted paths.

Note:

Both __init__.py files are intentionally empty.
 I'm generating them now as their own files (rather than skipping them) so the "generate one file at a time, never skip files" rule is honored literally — even trivial scaffolding files get their explicit turn
 """
